name: Deploy site

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      # existing steps
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
